<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:background="@drawable/bg_red_alert"
    android:orientation="horizontal"
    android:padding="12dp"
    tools:background="@drawable/bg_red_alert">

    <ImageView
        android:id="@+id/alertIcon"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginTop="2dp"
        android:layout_marginEnd="8dp"
        android:src="@drawable/ic_alert_triangle"
        android:tint="@color/red_500"
        tools:tint="@color/red_500" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/alertTitleText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="@color/red_400"
                android:textSize="14sp"
                android:textStyle="bold"
                tools:text="Evacuation Notice"
                tools:textColor="@color/red_400" />

            <TextView
                android:id="@+id/alertTimeText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/gray_400"
                android:textSize="12sp"
                tools:text="10 min ago" />
        </LinearLayout>

        <TextView
            android:id="@+id/alertDescriptionText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/red_300"
            android:textSize="12sp"
            tools:text="Riverside community - Water levels rising rapidly. Immediate evacuation recommended."
            tools:textColor="@color/red_300" />
    </LinearLayout>
</LinearLayout>

